import getModelAttrs from 'ember-london-talk/utils/get-model-attrs';
import { module, test } from 'qunit';

module('Unit | Utility | get model attrs');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = getModelAttrs();
  assert.ok(result);
});
